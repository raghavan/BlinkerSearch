crawler.py
The crawler file has the seed_url coded inside it. To run the crawler enter python crawler.py

searchengine.py
Once the web pages are indexed, the searchengine.py can be run using python searchengine.py command, it will ask for the keyword to be searched and returns the webpages where the keywords are present.
